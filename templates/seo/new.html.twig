{% extends '_partials/main.html.twig' %}

{% block title %}Ajouter un référencement{% endblock %}
{% block subtitle %}Ajouter un référencement{% endblock %}
{% block content %}
    <section class="forms">
        <div class="container-fluid">
             {# <a href="{{ path('app_seo_index') }}">back to list</a> #}
            <!-- Page Header-->
            <header> 
                <h1  style="text-align: center;">Ajouter un référencement</h1>
            </header>
            <div class="row justify-content-md-center"> 
                <div class="col-lg-12">
                    <div class="card">
                        <div class="card-body">

                            {{ include('seo/_form.html.twig') }}

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

{% endblock %}
