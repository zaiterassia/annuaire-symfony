{% extends '_partials/main.html.twig' %}

{% block title %}Modifier un référencement{% endblock %}
{% block subtitle %}Modifier un référencement{% endblock %}

{% block content %}
    <section class="forms">
        <div class="container-fluid">
             {# <a href="{{ path('app_seo_index') }}">back to list</a> #}
            <!-- Page Header-->
            <header> 
                <h1  style="text-align: center;">Modifier un référencement</h1>
            </header>
            <div class="row justify-content-md-center"> 
                <div class="col-lg-12">
                    <div class="card">
                        <div class="card-body">

                            {{ include('seo/_form.html.twig') }}

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

{% endblock %}



{# {% extends 'base.html.twig' %}

{% block title %}Edit Seo{% endblock %}

{% block body %}
    <h1>Edit Seo</h1>

    {{ include('Seo/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_Seo_index') }}">back to list</a>

    {{ include('Seo/_delete_form.html.twig') }}
{% endblock %} #}
