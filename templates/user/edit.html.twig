{% extends '_partials/main.html.twig' %}

{% block title %}Modifier un utilisateur{% endblock %}
{% block subtitle %}Modifier un utilisateur{% endblock %}

{% block content %}
    <section class="forms">
        <div class="container-fluid">
             {# <a href="{{ path('app_seo_index') }}">back to list</a> #}
            <!-- Page Header-->
            <header> 
                <h1  style="text-align: center;">Modifier un utilisateur</h1>
            </header>
            <div class="row justify-content-md-center"> 
                <div class="col-lg-12">
                    <div class="card">
                        <div class="card-body">

                            {{ include('user/_form.html.twig') }}

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

{% endblock %}